# graphql-uploader



Include settings.json to root directory

Example:

```json
{
  "mongoUri": "MONGO_URI",
  "s3": {
    "bucketName": "BUCKET_NAME",
    "iamUserKey": "USER_KEY",
    "iamUserSecret": "USER_SECRET"
  }
}
```
