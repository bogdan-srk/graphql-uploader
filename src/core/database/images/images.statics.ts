import { IImage, IImageDocument, IImageModel } from './images.types';

// export async function create(
//   this: IImageModel,
//   imageData: IImage,
// ): Promise<IImageDocument> {
//
//   return this.create(imageData);
// }
